// Firebase Storage requires the Blaze billing plan, which isn't enabled yet.
// Flip this to true once Storage is turned on in the Firebase console —
// no other code changes needed, FileUpload picks this up automatically.
export const DOCUMENT_UPLOADS_ENABLED = false;
